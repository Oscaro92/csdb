// * import lib
import React from "react";

// * import component
import Header from '../../components/Header'
import Footer from '../../components/Footer'

const SDB = () => {
    return (

        <div>
            <Header></Header>
            <h1>SDB</h1>
            <Footer></Footer>
        </div>
    );
};

export default SDB;